# Tools

Prefer these tools when available:

- **`ast-grep`** — use instead of `grep` for structural code searches (finding function definitions, call sites, class declarations, etc.) when the language is supported
- **`sd`** — use instead of `sed` for find-and-replace operations in files
- **`yq`** — use for reading and editing YAML files instead of manual text edits

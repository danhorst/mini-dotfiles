# Linux/WSL-only Homebrew packages.
# (No clipboard helper needed under WSL; the bridge is clip.exe via Windows interop.)

brew "socat"  # WSL SSH relay: bridges Windows OpenSSH agent to a Unix socket via npiperelay.exe
